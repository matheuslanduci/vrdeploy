# Arquivo .env.docker para UI - Variáveis para execução com Docker
# Este arquivo contém as configurações de ambiente para a aplicação Angular

# API Configuration - URLs internas do Docker
API_URL="http://api:3000/api"
WS_URL="ws://api:3000/pubsub"

# External URLs (quando acessando de fora do Docker)
# Descomente estas linhas se quiser usar URLs externas
# API_URL="http://localhost:3000/api" 
# WS_URL="ws://localhost:3000/pubsub"

# Build Configuration
NODE_ENV="production"